# Cloudflare Worker config for the MAISON D'VUE Live Chat AI proxy.
# See chat-worker/README.md for the full deploy walkthrough.

name = "maisondvue-chat"
main = "worker.js"
compatibility_date = "2024-11-01"

# The Anthropic API key is NOT stored here. It is set as an encrypted secret:
#   npx wrangler secret put ANTHROPIC_API_KEY
